@{
    ViewData["Title"] = "403 - Доступ запрещен";
    Layout = "_AdminLayout";
}

<div class="error-container">
    <div class="error-content">
        <h1 class="error-code">403</h1>
        <h2 class="error-title">Доступ запрещен</h2>
        <p class="error-message">Извините, у вас нет доступа к этой странице.</p>
        <a href="/admin" class="btn btn-primary">Вернуться в админ панель</a>
    </div>
</div>

<style>
    .error-container {
        display: flex;
        justify-content: center;
        align-items: center;
        min-height: 80vh;
        text-align: center;
        padding: 20px;
    }

    .error-content {
        max-width: 600px;
    }

    .error-code {
        font-size: 120px;
        font-weight: bold;
        color: #dc3545;
        margin: 0;
        line-height: 1;
    }

    .error-title {
        font-size: 32px;
        margin: 20px 0;
        color: #343a40;
    }

    .error-message {
        font-size: 18px;
        color: #6c757d;
        margin-bottom: 30px;
    }

    .btn-primary {
        padding: 12px 30px;
        font-size: 16px;
        border-radius: 5px;
        text-decoration: none;
        transition: all 0.3s ease;
    }

    .btn-primary:hover {
        transform: translateY(-2px);
        box-shadow: 0 4px 8px rgba(0,0,0,0.1);
    }
</style> 